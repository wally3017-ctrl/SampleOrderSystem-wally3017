# 개발 계획 (PLAN.md)

> 반도체 시료 생산주문관리 시스템 — SampleOrderSystem

---

## 개발 원칙

- **아키텍처**: MVC 패턴 (Model / Controller / View 패키지 분리)
- **테스트**: 각 Phase 완료 시 **gmock** 기반 UnitTest 수행
- **데이터 영속성**: 애플리케이션 재시작 후에도 데이터 유지
- **스케줄링**: 생산라인 FIFO
- **빌드**: MS Build 옵션 변경 금지

---

## Phase 1 — 프로젝트 기반 구축

**목표**: MVC 스켈레톤, 데이터 영속성, 콘솔 입출력 기반 마련

### 작업 항목

| 항목 | 설명 |
|------|------|
| 프로젝트 구조 생성 | `Model` / `Controller` / `View` / `Repository` 패키지 분리 |
| 메인 진입점 | `Program.cs` — 메인 루프, 메뉴 라우팅 |
| 콘솔 I/O 유틸 | 입력 처리, 화면 출력 공통 헬퍼 |
| 데이터 영속성 | JSON 파일 기반 저장·불러오기 구현, CRUD 공통 인터페이스 정의 |
| Dummy 데이터 생성 Tool | 테스트용 초기 데이터 자동 생성 |
| 메인 메뉴 화면 | 시스템 현황 요약(등록 시료 수, 총 재고, 전체 주문 수, 생산라인 대기 수) 표시 |

### 산출물

- `Model/` — 도메인 모델 기반 클래스
- `Repository/` — JSON 기반 데이터 저장소
- `View/MainMenuView` — 메인 메뉴 화면
- `Controller/MainController` — 메뉴 라우팅

### gmock 테스트 항목

- Repository CRUD 동작 검증 (저장 → 불러오기 → 수정 → 삭제)
- 잘못된 입력값에 대한 예외 처리 검증
- 메인 메뉴 라우팅 분기 검증

---

## Phase 2 — 시료 관리 (Sample Management)

**목표**: 시료 등록·조회·검색 기능 완성

### 작업 항목

| 항목 | 설명 |
|------|------|
| 시료 모델 | `Sample` — ID, 이름, 평균 생산시간(min/ea), 수율, 재고 수량 |
| 시료 등록 | 시료 ID 중복 검사 후 등록 |
| 시료 목록 조회 | 등록된 전체 시료 + 현재 재고 수량 표시 |
| 시료 검색 | 이름 등 속성으로 부분 검색 |
| 시료 Repository | JSON 기반 CRUD 구현 |

### 도메인 규칙

- 수율 = 정상 시료 수 / 총 생산 시료 수 (0 < 수율 ≤ 1)
- 시료 ID는 시스템 내 유일해야 함
- 등록된 시료만 주문 가능

### gmock 테스트 항목

- 시료 등록 성공 / 중복 ID 등록 실패 검증
- 시료 목록 조회 — 빈 목록, 1건, 다건 케이스
- 시료 검색 — 일치 / 부분 일치 / 결과 없음 케이스
- 수율 경계값(0, 1, 초과값) 입력 처리 검증

---

## Phase 3 — 주문 관리 (Order Management)

**목표**: 주문 접수 → 승인/거절 → 상태 전이 로직 완성

### 작업 항목

| 항목 | 설명 |
|------|------|
| 주문 모델 | `Order` — 주문번호, 시료 ID, 고객명, 주문 수량, 상태, 접수 일시 |
| 주문 번호 생성 | `ORD-YYYYMMDD-NNNN` 형식 자동 생성 |
| 시료 주문 (접수) | 시료 ID / 고객명 / 주문 수량 입력 → 상태 **RESERVED** |
| 주문 승인 | RESERVED → 재고 확인 → **CONFIRMED** 또는 **PRODUCING** 자동 분기 |
| 주문 거절 | RESERVED → **REJECTED** 즉시 전환 |
| 주문 Repository | JSON 기반 CRUD, 상태별 조회 |

### 도메인 규칙

- 승인 시 재고 ≥ 주문 수량 → 즉시 **CONFIRMED**
- 승인 시 재고 < 주문 수량 → 생산라인 자동 등록 + **PRODUCING**
- 거절된 주문(REJECTED)은 이후 모든 흐름에서 제외

### gmock 테스트 항목

- 주문 접수 성공 / 존재하지 않는 시료 ID로 접수 실패 검증
- 재고 충분 시 승인 → CONFIRMED 전환 검증
- 재고 부족 시 승인 → PRODUCING 전환 + 생산라인 등록 검증
- 거절 → REJECTED 전환 검증
- 주문 번호 형식 및 순번 중복 없음 검증

---

## Phase 4 — 생산라인 (Production Line)

**목표**: FIFO 생산 큐, 실 생산량 계산, 생산 완료 후 상태 전이 구현

### 작업 항목

| 항목 | 설명 |
|------|------|
| 생산 작업 모델 | `ProductionJob` — 주문 ID, 시료 ID, 부족분, 실 생산량, 총 생산 시간, 등록 일시 |
| FIFO 생산 큐 | 선입선출 대기열 관리 |
| 실 생산량 계산 | `ceil(부족분 / (수율 × 0.9))` |
| 총 생산 시간 계산 | `평균 생산시간 × 실 생산량` |
| 생산 완료 처리 | PRODUCING → **CONFIRMED** 자동 전환, 재고 수량 증가 |
| 생산라인 현황 화면 | 현재 처리 중인 주문 정보, 진행률, 완료 예정 시간, 대기 큐 목록 표시 |

### 도메인 규칙

- 단일 생산라인 — 동시에 하나의 작업만 처리
- 스케줄링 전략: **FIFO** (선입선출)
- 생산 완료 시 해당 주문 상태 PRODUCING → CONFIRMED로 전환

### gmock 테스트 항목

- 실 생산량 계산식 검증 (`ceil(부족분 / (수율 × 0.9))`) — 다양한 수율/부족분 조합
- FIFO 순서 보장 검증 (여러 작업 등록 후 순서 확인)
- 생산 완료 시 PRODUCING → CONFIRMED 전환 및 재고 반영 검증
- 생산라인 비어 있을 때 현황 조회 검증

---

## Phase 5 — 모니터링 및 출고 처리

**목표**: 모니터링 대시보드, 출고 처리 기능 완성 및 전체 흐름 통합

### 작업 항목

| 항목 | 설명 |
|------|------|
| 모니터링 — 주문량 | 상태별(RESERVED / CONFIRMED / PRODUCING / RELEASED) 주문 수 표시 (REJECTED 제외) |
| 모니터링 — 재고량 | 시료별 재고 수량 + 상태 표기 (여유 / 부족 / 고갈) |
| 재고 상태 판단 | 여유: 주문 대비 재고 충분 / 부족: 재고 부족 / 고갈: 재고 0 |
| 출고 처리 | CONFIRMED 주문 목록 조회 → 선택 → **RELEASED** 전환 |
| 전체 흐름 통합 테스트 | Phase 1~4 연계 시나리오 검증 |

### 도메인 규칙

- 모니터링에서 REJECTED 주문은 완전 제외
- 출고는 CONFIRMED 상태 주문에 대해서만 실행 가능
- 출고 완료 시 상태: CONFIRMED → **RELEASED**

### gmock 테스트 항목

- 상태별 주문 수 집계 — REJECTED 제외 검증
- 재고 상태 분류 검증 (여유 / 부족 / 고갈 경계값)
- 출고 처리 — CONFIRMED → RELEASED 전환 검증
- 출고 대상 없을 때 처리 검증
- **통합 시나리오**: 주문 접수 → 승인(재고 부족) → 생산 완료 → 출고 처리 전체 흐름

---

## 개발 일정 요약

| Phase | 내용 | 테스트 |
|-------|------|--------|
| Phase 1 | 프로젝트 기반 구축 (MVC, 데이터 영속성) | gmock — Repository CRUD, 메뉴 라우팅 |
| Phase 2 | 시료 관리 (등록/조회/검색) | gmock — 시료 CRUD, 검색, 수율 경계값 |
| Phase 3 | 주문 관리 (접수/승인/거절) | gmock — 상태 전이, 재고 분기 로직 |
| Phase 4 | 생산라인 (FIFO 큐, 생산량 계산) | gmock — 계산식, FIFO 순서, 상태 전이 |
| Phase 5 | 모니터링 + 출고 처리 + 통합 | gmock — 집계, 분류, 전체 시나리오 |
